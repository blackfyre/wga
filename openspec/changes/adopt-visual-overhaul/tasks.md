## 1. Release contract and existing-plan reconciliation

- [x] 1.1 Audit every non-development screen, component, script, and stated interaction in `wga-visual-overhaul` against the production application and record the owning capability.
- [x] 1.2 Review and update the unimplemented visitor-itineraries OpenSpec artefacts against this change's itinerary requirements.
- [x] 1.3 Review and update the unimplemented artwork-relationship-paths OpenSpec artefacts against the four-basis public relationship contract and artist selections.
- [x] 1.4 Review and update the unimplemented thumbnail OpenSpec artefacts against the `wga-src` staged-media contract.
- [x] 1.5 Establish the release acceptance inventory for all reference routes, responsive viewports, and regular-visitor and scholar journeys.

## 2. Real-data and staged-media release path

- [x] 2.1 Define the versioned hand-off from the approved `wga-src` production SQLite output and staged storage tree to WGA release data.
- [x] 2.2 Document and verify the manual release-provisioning step that installs the approved real database at the existing `WGA_SEED_SQLITE_PATH` before WGA starts, without adding a WGA runtime manifest setting or changing the seed importer.
- [x] 2.3 Document producer-manifest validation and manual pre-population of the complete PocketBase storage tree, with recorded evidence before WGA starts and records reference preseeded assets.
- [x] 2.4 Carry original image dimensions from `wga-src` into WGA, extend named delivery-profile constants and handler mappings across all thirteen approved widths, and use the original URL whenever the assigned width would upscale or dimensions are unavailable.
- [x] 2.5 Define the manual provisioning evidence for manifest hashes, installed database and storage destinations, published originals, source dimensions, source-eligible staged downscales, colour profiles, and actionable failures without requiring upscaled derivatives.
- [x] 2.6 Exercise and record the provisioning runbook's positive and negative checks for missing or unapproved external seed configuration, preseeded asset availability, and eligible variants, and add focused application tests for original fallback and prevention of PocketBase on-demand upscaling without introducing WGA manifest configuration.
  - Verification (2026-08-20): all nine temporary runbook cases produced their expected pass/fail result, including missing and mismatched seeds, missing originals, missing eligible `700x0`, and absent equal/larger profiles accepted as original-only.
  - Verification (2026-08-20): WGA focused URL, seed, and config suites passed 32, 15, and 29 tests respectively; producer sourcebundle, thumbnail, and schema suites passed 3, 5, and 2 tests respectively; focused `go vet` and both repository diff checks passed.
  - Verification (2026-08-20): runtime source and configuration contain no `WGA_SEED_MANIFEST_PATH`; no live provisioning or infrastructure mutation was performed.

## 3. Shared public foundation

- [x] 3.1 Port the Rams-inspired theme tokens, responsive tiers, relative type scale, and dark theme into the production CSS source.
  - Verification (2026-08-20): `bun run build:css` and `git diff --check -- resources/css/style.pcss` passed; generated CSS contains the 720px/1080px tiers, all 33 relative type tokens, and complete light/dark Rams theme values.
  - Verification (2026-08-20): independent review confirmed the reference values match, existing `wga_light`/`wga_dark` bootstrap behaviour remains compatible, and the change is confined to the production CSS source.
- [ ] 3.2 Rebuild the shared layout header, primary navigation, MORE destinations, footer, and responsive disclosure from the reference.
  - Partial verification (2026-08-20): the shared shell subset passed Templ generation, 15 focused component/layout tests, CSS compilation, scoped diff checks, and Chromium closed/open mobile-disclosure observations.
  - Partial verification (2026-08-24): Artists, Glossary, Guestbook, and Inspiration now distinguish page-level `mc-area` requests from feature-local fragments. Raw response matrices, repeated Dual exit/re-entry and Back/Forward journeys, both responsive EXIT controls, all implemented shared destinations, and JavaScript-disabled exit preserved exactly one `#mc-area` with correct titles and no HTMX errors; independent review approved the repair, `go test ./... -cover` passed 1,189 tests across 53 packages, and `go vet ./...` passed.
  - Dependency: final completion remains gated on the real `/tours` handler owned by task 8.4 and resolution of the shared `/postcard` destination, whose current route requires a recipient token; no temporary route stubs were added. Timeline and itinerary destinations are now implemented.
- [x] 3.3 Implement shared page heads, section rules, work cards/rows, metadata lists, plates, fields, chips, range controls, and empty states.
  - Verification (2026-08-20): Templ generation, 18 focused component/DTO tests, focused `go vet`, CSS compilation, and scoped diff checks passed; generated files remain ignored.
  - Verification (2026-08-20): independent review confirmed reference-aligned component structure and Rams type tokens, accessible labelled fields/chips/dual ranges, complete image-URL hand-off, a no-JavaScript zoom fallback, and no viewer hooks on navigational work cards/rows.
- [x] 3.4 Implement theme preference persistence and the no-flash document-head behaviour.
  - Verification (2026-08-20): frontend build, Templ generation, 26 focused layout/component tests, focused `go vet`, Biome lint, scoped diff checks, and nine isolated Playwright theme tests passed.
  - Verification (2026-08-20): stylesheet-held browser checks confirmed localStorage, legacy-value, cookie-only, blocked-storage, and OS-preference paths select `wga-rams`/`wga-rams-dark` before CSS; explicit choices persist in localStorage and the `wga_theme` cookie, while clear resumes OS tracking.
- [x] 3.5 Align bionic-reading persistence, footer control, prose scope, and HTMX lifecycle behaviour with the release specification.
  - Verification (2026-08-20): 174 Go tests across 23 packages, focused request-context/footer tests, `go vet`, Templ generation, frontend build, Biome checks, and scoped diff checks passed.
  - Verification (2026-08-20): actual-bundle browser fixtures confirmed server cookie marking, hidden no-JavaScript control, localStorage/cookie persistence, blocked-storage session toggling, lossless scoped prose transformation, semantic exclusions, and idempotent HTMX swap handling.
- [x] 3.6 Replace or adapt shared dialogs and the artwork viewer to meet the release modal, focus, keyboard, and reduced-motion contract.
  - Verification (2026-08-20): shared-dialog and deliberate ViewerJS browser checks confirmed labelled modal surfaces, deliberate initial focus, Tab containment, background inertness, visible and Escape dismissal, invoker restoration including removed-invoker cleanup, keyboard/click viewer activation, supplied zoom-URL use, reduced-motion handling, and the ordinary artwork zoom-link fallback without viewer hooks on browsing cards.
  - Verification (2026-08-20): `templ generate`, 50 focused Go tests across shared components/layouts and artwork/feedback handlers, `go vet ./...`, `mise run app:build`, focused Biome checks, `git diff --check`, built-asset probes, and 9 targeted Chromium tests passed.
- [x] 3.7 Implement the cookie notice and preference treatment without changing consent semantics.
  - Verification (2026-08-20): built-dist Chromium checks confirmed truthful necessary-only first-visit consent, persisted dismissal, returning-visitor preference access through `CookieConsent.validConsent()`, read-only necessary preferences, privacy routing, hidden unavailable-script/failed-initialisation controls, responsive and 200%-text-safe notice geometry, dark theme, reduced motion, visible/Escape dismissal, and feedback separation without adding analytics or changing cookie semantics.
  - Verification (2026-08-20): `templ generate`, 32 focused Go tests, `go vet ./...`, `go test ./... -cover` (397 tests across 48 packages), frontend and application builds, focused Biome checks, `git diff --check`, and 3 targeted Chromium tests passed.
- [x] 3.8 Add shared responsive, keyboard, dark-theme, enlarged-text, and no-JavaScript coverage for the public shell.
  - Verification (2026-08-21): the built-dist Chromium matrix passed 51 tests across the focused public-shell, navigation, title-scale, theme, bionic-reading, cookie-consent, feedback, and artwork suites, covering 390px/834px/1440px composition, keyboard-only operation, Rams dark roles, 390px/200% text, equivalent 400% reflow, and JavaScript-disabled disclosure and preference honesty.
  - Verification (2026-08-21): footer-local shrink and wrap constraints removed the proven 390px/200% shared-footer overflow without clipping or changing the two-column/four-column tiers; client-only appearance controls now remain server-hidden until successful JavaScript initialisation.
  - Verification (2026-08-21): `templ generate`, 33 focused component/layout tests, `go vet ./...`, `bun run build`, focused Biome checks, `git diff --check`, direct 200%/400% geometry probes, and independent scope review passed. Task 3.2 remains separately gated on the real deferred route handlers.

## 4. Keyboard and reference interactions

- [x] 4.1 Implement the single server-rendered keyboard screen registry for all release destinations and section numbers.
  - Verification (2026-08-21): focused registry tests and built Chromium checks confirmed the exact 17-destination contract, unique two-digit numbers/routes/letters, reserved J/K/H/L movement keys, HOME `O` and GLOSSARY `Y`, final deferred route hrefs, lowercase JSON shape, and one server table feeding JSON, palette, and help without a TypeScript destination list.
  - Verification (2026-08-21): `templ generate`, 32 focused component/keyboard-handler tests, `go vet ./...`, frontend build, Biome on the changed browser spec, `git diff --check`, and 4 registry browser tests passed. The full keyboard spec exposed one pre-existing hidden-mobile search-focus defect assigned to task 4.2; all 8 other tests passed.
- [x] 4.2 Implement shortcut help, command palette, global search focus, Escape dismissal, and responsive mobile-search behaviour.
  - Verification (2026-08-21): current built-dist checks confirmed labelled native help/palette focus and restoration, truthful registry/action guidance, bounded suggestion filtering, keyboard selection, platform keycaps, desktop/mobile visible-search focus, editable-control safety, outside-click/mobile Escape, and native Escape ownership for shared dialogs, ViewerJS, and the dual lookup.
  - Verification (2026-08-21): `templ generate`, 32 focused component/keyboard-handler tests, `go vet ./...`, frontend and binary builds, focused Biome checks, `git diff --check`, 12 keyboard browser tests, 9 feedback/artwork modal regressions, 2 no-JavaScript shell checks, and direct shared-dialog/viewer lifecycle probes passed against verified current asset hashes.
- [x] 4.3 Implement opted-in result-list caret traversal and HTMX replacement reset behaviour.
  - Verification (2026-08-21): built-dist checks confirmed visible opt-in list selection, responsive row movement with Arrow/J/K, single-record movement with Arrow/H/L, first-mark and clamped bounds, navigable Enter, non-navigable guestbook markers, Home/no-list default scrolling, Escape clearing, reduced-motion scrolling, and ordinary no-JavaScript links.
  - Verification (2026-08-21): two real artwork HTMX replacements cleared stale caret state and restarted at index 0 without duplicate listeners; Biome, frontend/application builds, `go vet ./...`, `go test ./... -cover` (399 tests across 48 packages), `git diff --check`, 16 keyboard tests, and 15 modal/viewer/public-shell regressions passed.
- [x] 4.4 Implement glossary-term and help-tip components with keyboard-visible in-place definitions.
  - Verification (2026-08-21): component and current-CSS Chromium checks confirmed plain escaped definitions, focusable non-action semantics, complete accessible names, aria-hidden visual duplicates, Bionic opt-out, dotted glossary treatment, hover/focus/blur parity, wide glossary-above/help-below placement, and viewport-contained fixed-inset narrow presentation at right/left edges and 200% text.
  - Verification (2026-08-21): `templ generate`, 4 focused component tests including escaped script-like input, `go vet ./...`, frontend/application builds, focused Biome, `git diff --check`, 4 no-JavaScript/dark/reduced-motion browser tests, current CSS hash verification, and direct 390px geometry probes passed.
- [x] 4.5 Add route, keyboard-only, editable-control, palette-limit, and help-surface regression tests.
  - Verification (2026-08-21): exact 17-route registry parity, help/palette keyboard-only operation and focus restoration, input/textarea/select/contenteditable isolation, minimum-query and remaining-capacity requests, nine-row palette containment, suggestion-failure fallback, responsive traversal, HTMX reset, and shared glossary/help accessibility regressions passed in 25 Chromium tests.
  - Verification (2026-08-21): fixed the discovered zero-capacity PocketBase query defect so artist-filled budgets do not trigger an unbounded artwork query; 7 focused handler tests, 405 Go tests across 48 packages, vet, build, Biome, diff checks, strict OpenSpec validation, a live seven-row `limit=7` response, a capped ten-row `limit=999` response, and 15 feedback/artwork/public-shell Chromium regressions passed against hash-matched built assets.

## 5. Discovery and reference pages

- [x] 5.1 Implement the home route with its collection argument, work of the day, counts, recent additions, and discovery routes.
  - Verification (2026-08-21): focused repository, workflow, route, and template tests confirmed published counts, first-published-author eligibility, deterministic UTC-day selection through a bounded count plus one-row offset query, four-row newest-first recent additions, 900/500 source-eligible image URLs, safe empty states, canonical metadata, semantic markup, and exact ordinary `/artists`, `/artworks`, and `/inspire` discovery links.
  - Verification (2026-08-21): 417 Go tests across 48 packages, vet, Templ/frontend/application builds, Biome, diff checks, and strict OpenSpec validation passed; built-dist Chromium passed 7 home, 6 public-shell, and 1 default-key tests across 390/834/1440, light/dark, reduced motion, 200% text, keyboard and no-JavaScript states with hash-matched HTTP-200 assets.
- [x] 5.2 Implement the Inspiration route with published-work discovery and routes to tours and itineraries.
  - Verification (2026-08-21): focused persistence, route, and template tests proved a fixed ten-row shuffled query, published artwork plus published first-author eligibility (including direct missing-author exclusion), canonical record links, 500px/no-upscale delivery, no-image and empty-state handling, canonical metadata, ordinary `/inspire` refresh, and exact `/tours` and `/itineraries` distinctions without ViewerJS.
  - Verification (2026-08-21): 424 Go tests across 48 packages, vet, Templ/frontend/application builds, Biome, diff checks, and strict OpenSpec validation passed; hash-matched built-dist assets served HTTP 200 and 29 Chromium tests passed for Inspiration, public-shell, and keyboard behaviour across 390/834/1440, 1/2/4 columns, dark/reduced-motion, 200% text, keyboard, and no-JavaScript states.
- [x] 5.3 Implement About, Contributors, privacy/reference pages, and public error states in the shared presentation system.
  - Verification (2026-08-21): focused handler, TOC, contributor, utility, and template tests confirmed route-derived About/privacy/general kickers, managed content and deterministic unique heading anchors, canonical/push metadata, Reader-only contributor snapshots with safe optional URLs/source headers, expected 404 versus redacted 500 mapping, shared Bionic-aware error layouts, and public-GET fallback isolation from API/admin/assets/sitemap/preview and non-GET boundaries.
  - Verification (2026-08-21): 499 Go tests across 48 packages, vet, Templ/frontend/application builds, Biome, diff checks, and strict OpenSpec validation passed; hash-matched built assets served HTTP 200 and 36 Chromium tests passed for reference pages, real public/API misses, public shell, privacy consent links, and keyboard behaviour across 390/834/1440, light/dark, reduced motion, keyboard, no-JavaScript, and 200%/400% text reflow.
- [x] 5.4 Implement the Statistics route with accessible charts, equivalent tables, captions, and real-data figures.
  - Verification (2026-08-21): focused persistence, handler, template, and current built-dist browser checks confirmed published-record aggregation, unpublished-author exclusion, identical art-form and school-period figures across Chart.js inputs and all three server-rendered tables, visible captions, expanded school labels, Rams-token category swatches, and canonical route metadata.
  - Verification (2026-08-21): 34 focused Go tests, full vet, Templ/frontend/binary builds, Biome, diff checks, and 15 asset-matched Chromium tests passed; the browser matrix proved actual light/dark canvas redraw, disabled reduced-motion animation, populated no-JavaScript tables, safe chart lifecycle behaviour, and responsive chart-before-table order without page overflow at 390px, 834px, and 1440px.
- [x] 5.5 Add server-rendered and responsive tests for home, inspiration, statistics, and reference destinations.
  - Verification (2026-08-21): route, handler, workflow, and template coverage collectively proved server-rendered Home, Inspiration, Statistics, About, privacy, Contributors, and public reference/error content, links, metadata, empty states, and no-JavaScript behaviour; 105 focused Go tests and affected-package vet passed.
  - Verification (2026-08-21): a fresh asset-matched isolated build passed 33 combined Chromium tests across the four discovery/reference suites, covering 390px/834px/1440px composition and document overflow, Inspiration 1/2/4-column layout, narrow 200% reflow, dark and reduced-motion states, populated Statistics table fallbacks, and ordinary no-JavaScript navigation. The initial incomplete-seed failures were traced to local Garage being unavailable and did not reproduce after a clean isolated seed.

## 6. Artist, selection, and artwork records

- [x] 6.1 Rebuild the artist index with alphabet navigation, filters, birth-year range, grid/table views, and unavailable-record treatment.
  - Verification (2026-08-21): focused persistence, handler, integration, and template checks confirmed published-only bounded queries, combined name/letter/school/period/birth filters, deterministic A–Z/Z–A/birth sorting, canonical state-preserving URLs, clamped ranges/pages, grid/table views, 500-profile/original portrait resolution, escaped Unicode-safe highlighting, and visible non-linked `NOT DIGITISED` records whose unpublished works do not confer availability.
  - Verification (2026-08-21): 558 Go tests across 48 packages, vet, Templ/frontend/application builds, Biome, diff checks, and strict OpenSpec validation passed; hash-matched built-dist Chromium passed all 11 artist-index tests across 390px/834px/1440px, dark/reduced-motion, 200% text, keyboard/HTMX, and ordinary no-JavaScript operation without document overflow.
- [x] 6.2 Rebuild artist records with metadata, biography, glossary terms, portraits, works, period music, and citations.
  - Verification (2026-08-21): focused persistence, handler, integration, template, glossary, JSON-LD, and URL tests confirmed published-only artist/works access, canonical full/HTMX responses, sanitised glossary-decorated biography on record and legacy Dual Mode paths, targeted bounded school/period/glossary reads, truthful metadata, 600-profile/original portrait and identity parity, deterministic twelve-work preview/count, no-autoplay named-window period music, and canonical visible BibTeX citation.
  - Verification (2026-08-21): 612 Go tests across 48 packages, vet, Templ/frontend/application builds, Biome, diff checks, strict OpenSpec validation, and reproducible asset-matched CSS passed; fresh built-dist Chromium passed all 23 artist-record/glossary/help tests across 390px/834px/1440px, dark/reduced-motion, 200% text, keyboard/HTMX, JavaScript-disabled use, publication denial, and focused tooltip containment. Independent assurance accepted the task after the 390px retained-transform, legacy sanitisation, and unbounded-read regressions were closed.
- [x] 6.3 Add selection data contracts and routes for source-backed curated artist selections using the producer's deterministic artist-and-selection identity.
  - Verification (2026-08-23): migration, seed, repository, handler, and route coverage proved persisted source/content provenance, canonical producer path and deterministic identity validation, exact ordered artwork membership, published artist-scoped reads, non-destructive rollback, and fail-closed relation contracts before external import.
  - Verification (2026-08-23): 264 affected tests and 983 full-suite tests across 52 packages passed with full vet, affected builds, diff checks, and independent review; task 6.4 remains separately responsible for previews, reading presentation, missing-commentary treatment, and citations.
- [x] 6.4 Implement selection previews, dedicated reading pages, honest missing-commentary states, and selection citations.
  - Verification (2026-08-24): independent real-source runtime imported 1,307 published selections and proved ordered artist-scoped previews, selected/catalogued counts, dedicated reading routes, representative and wider-holding links, supplied commentary, honest missing-commentary states, sixteen-work rendering, and BibTeX citations without generated prose. Focused, race, full-suite, no-JavaScript, responsive, keyboard, vet, build, Templ, frontend, and diff checks passed.
- [x] 6.5 Rebuild artwork records with deliberate plates, reproduction provenance, metadata, an image-derived palette, commentary, music, and citations. Tone-keyword presentation is deferred until an authoritative source dataset is available.
  - Verification (2026-08-24): independent real-source verification passed shared deliberate Plates with 1400 display/2000 zoom and original-file links, dimensions/source path, allow-listed canonical HTTPS WGA provenance with safe external-link policy, explicit absent licence/commentary states, image-derived palette, named period music without autoplay, citation, and fail-closed malformed/deceptive source URLs. Record/route/template/race/full-suite, vet, build, Templ, no-JavaScript and responsive checks passed against the 52,866-artwork producer import.
- [x] 6.6 Add focused handler, workflow, route, and no-JavaScript tests for records and selections.
  - Verification (2026-08-24): two deterministic embedded-only synthetic selections exercise supplied and missing commentary, ordered membership, provenance, hashes, previews, dedicated routes, counts, BibTeX citations, and ordinary holding links without affecting external producer imports. A fresh default-data JavaScript-disabled Playwright run passed 4/4 with no skips; 53 focused and 1,365 full-suite tests, vet, frontend build, Biome, Templ formatting/generation, integrity, and diff checks passed with independent approval.

## 7. Catalogue, relationships, and comparison

- [x] 7.1 Extend artwork search filters for period, technique, and location while preserving URL state and progressive enhancement. Tone-keyword filtering is deferred until an authoritative source dataset is available.
  - Verification (2026-08-23): fixture-backed individual and combined period, technique, and location queries passed with canonical URL state, full-page/HTMX parity, and an ordinary no-JavaScript GET form; unknown direct values return an honest empty result rather than dropping the filter.
  - Verification (2026-08-23): bounded deterministic facets use stable ID tie-breaks, fetch one look-ahead value, expose at most their named limits, and report truncation only when values are omitted. Independent closure passed 74 catalogue handler tests, 88 page tests, focused vet, full build, and diff checks.
  - Reopened (2026-08-24): independent real-runtime verification found the deferred tone feature still persisted, imported, queried, retained in URL state, assembled as a facet, and rendered as a user-visible empty `TONE` control. The period/technique/location implementation remains accepted, but this task cannot close until tone behavior is absent end to end.
  - Final verification (2026-08-24): clean and legacy migration tests safely retired NULL/blank/`null`/empty-string/`[]`/`{}` tone fields while actionable fail-closed handling preserved genuinely non-empty values. A real 52,866-artwork import proved no tone schema, import, query, cache, URL state, facet, generated asset, or UI remained; period/technique/location fixture positives, canonical full/HTMX/no-JavaScript URL behavior, real technique/location results, and honest empty period results all passed.
- [x] 7.2 Implement catalogue sorting, direction labels, grid/dense-list views, and empty states.
  - Verification (2026-08-23): fixture-backed queries proved authoritative `source_row` archive ordering with unknown positions last in both directions, `date_start` ordering with unknown dates last, deterministic tie-breaks, explicit criterion-specific direction labels, URL-preserved grid/dense-list views, and honest empty states.
  - Verification (2026-08-23): 69 catalogue handler tests and 88 page tests passed with affected vet, full build, diff checks, and independent review; the remaining facet-order/truncation corrections belong only to task 7.1.
- [x] 7.3 Implement the four-basis related-work resolver using reviewed real-data relationships and palette profiles.
  - Verification (2026-08-24): independent real-source runtime proved deterministic published-only BY ARTIST author relations, SAME COLLECTION canonical public-museum relations, SAME PERIOD cross-artist works within forty years, and SIMILAR PALETTE producer `colour_signature` distances, each returning four truthful other records where available. Focused repository/handler/seed/delivery and race tests passed with the 1,344-test full suite, vet, build, and responsive browser checks.
- [x] 7.4 Implement related-work sparse-result explanations and basis-preserving URLs.
  - Verification (2026-08-24): independent real-runtime checks proved basis-preserving bookmarkable URLs and keyboard navigation, honest no-location and no-signature sparse explanations, documented alternative bases, ordinary no-JavaScript links, and published-only result handling at 390px, 834px, and 1440px without overflow.
- [x] 7.5 Rebuild Dual Mode as two complete, independently routed records with per-pane history, index, image size, and target routing.
  - Verification (2026-08-24): 61 focused and race-enabled Dual tests proved canonical independent pane paths, per-pane filters/sorts, opposite-pane state preservation, complete published index/artist/artwork rendering, same/other-pane targeting, shell/local HTMX boundaries, ordinary no-JavaScript links, and deterministic shareable URL state. Independent review approved the implementation and `go test ./... -cover` passed 1,225 tests across 53 packages with vet/build/Templ/frontend/Biome/diff checks.
- [x] 7.6 Implement the wide-mode URL override and pane container responsiveness for zoomed desktop use.
  - Verification (2026-08-24): source and focused tests confirmed persistent `wide=1` canonicalisation, normal narrow gates, pane-container layouts, assigned 700/1100/1600/2000 rendition behavior, and the labelled keyboard-scrollable metadata region. Integrated browser viewport and URL reload evidence remains assigned to task 7.7.
- [ ] 7.7 Add query, relation, sort, comparison-state, image-rendition, and browser regression coverage.

## 8. Timeline and guided editorial journeys

- [x] 8.1 Define and seed timeline lanes, density data, named spans, and published record links from approved art-period data and published artwork creation-date ranges; do not fabricate historical-event entries.
  - Verification (2026-08-24): real-import tests proved all 32 approved named periods, the `100–1994` period union, deterministic derivation from 52,866 dated published artworks across `101–1994`, correct single-year treatment for 966 records without end dates, bounded 48-mark/eight-card windows, lanes, density, counts, and canonical record links without fabricated historical events or prose. Sixty-four Timeline, nine template, sixty seed, and six seed-migration tests passed with focused vet, full build, Templ/diff checks, full-page/HTMX/fragment responses, ordinary no-JavaScript controls, and independent approval.
- [x] 8.2 Implement the server-rendered timeline brush, URL window, density strip, panels, mark geometry, and no-JavaScript controls.
  - Verification (2026-08-23): focused route/workflow/template tests proved deterministic URL windows, density and mark geometry, bounded disclosure, HTMX/full-page parity, and ordinary server-rendered range/link controls; the centrally registered `/timeline` route returned HTTP 200.
  - Verification (2026-08-23): independent integration acceptance passed at 390px, 834px, and 1440px without overflow, alongside focused/race suites, full vet/build, Templ checks, frontend build, and correct asset MIME types. Task 8.1 remains open because current producer output supplies no art-period rows.
- [x] 8.3 Define guided-tour, editor, page, contents, source, revision, and legacy-route data contracts.
  - Verification (2026-08-24): independent verification passed the private editor/tour/revision/page/section/block/list/bibliography/legacy-route contracts, textual tour numbers such as `6a`, required revision and page provenance, published active-revision reads, fail-closed malformed-record handling, safe record-derived canonical legacy routing, and non-destructive migration down/re-up behavior. Focused and race suites passed 128 Tour tests and four migration tests with clean Templ generation, build, frontend build, formatting, and diff checks.
- [x] 8.4 Implement the Guided Tours index, editorial filters, rebuilt/legacy distinction, and tour-versus-itinerary contract.
  - Verification (2026-08-24): independent review approved published and provenance-valid kind filtering, rebuilt/original grouping, textual tour numbers, derived rebuilt page counts, honest unavailable-scope treatment, fail-closed malformed-page exclusion, the explicit named-editor/shape/length/reading/lifetime tour-versus-itinerary facts, ordinary links, and correct full/shell/local HTMX boundaries with canonical pushed URLs. Focused source review passed 191 Go tests, vet, and nine Tour/viewer frontend tests.
- [x] 8.5 Implement title, text, picture, index, and sources tour pages with progress, contents rail, page turns, and deliberate plates.
  - Verification (2026-08-24): independent review approved stable addressed title/text/picture/list/sources pages, tour context, progress, stable-section contents, ordinary and local-HTMX page turns, safe sources/legacy destinations, contextual Arrow guidance, 1400 display and 2000/original zoom delivery, and shared deliberate plates. Final focused acceptance proved the fixed `h-[300px] md:h-[460px]` centred frame, contained image, image-level zoom source, no-JavaScript zoom href, placeholder/lazy behavior, credit, and full-record destination; 188 focused Tour/page/migration tests, vet, build, formatting, and diff checks passed.
- [ ] 8.6 Add timeline and tour data, route, keyboard, accessibility, and responsive tests.
  - Partial verification (2026-08-24): the Timeline portion passed fresh embedded-build browser acceptance at 390px, 834px, and 1440px plus 390px/834px at 200% text with exact viewport geometry, keyboard traversal, no-JavaScript forms, ordinary record links, real-seed integration, and independent approval. Guided Tours populated-page browser evidence remains outstanding.

## 9. Visitor itineraries

- [x] 9.1 Implement session-owned draft persistence, secure anonymous session identification, stop limit, and artwork-add workflow.
  - Verification (2026-08-23): security and race coverage proved strict 256-bit session tokens, production `__Host-` cookie policy, canonical-origin and trusted-client checks, cookie-rotation-resistant admission, one session draft, unique bounded stops, ordinary POST/HTMX additions, and zero durable records from cookie-less GETs.
- [x] 9.2 Implement the shared itinerary tray and its layout reservation across supported surfaces.
  - Verification (2026-08-23): shared request projection supplies CSRF/add state without GET persistence, resolves 120-profile tray images, renders supported card/record/timeline/Dual controls, and reserves the layout only for a non-empty tray; browser add, immediate update, reload persistence, clear, and no-JavaScript redirects passed.
- [x] 9.3 Rebuild the reference builder filmstrip and selected-stop narration editor with persisted ordering, bounded narration, removal, clear, and reload recovery.
  - Verification (2026-08-23): workflow, handler, migration, race, template, and integration suites passed for persisted ordering, bounded sanitised narration, ownership, removal, clear, concurrency constraints, and reload recovery.
  - Verification (2026-08-24): the add response now preserves the session-derived CSRF synchroniser through the out-of-band builder replacement. A regression test and isolated live HTMX lifecycle passed three distinct consecutive additions with counts 1→2→3 and stable non-empty tokens, exact reload order, subsequent metadata/narration/move/remove/clear mutations, fail-closed missing-CSRF handling, and an ordinary 303 no-JavaScript add; `go test ./... -cover` passed 1,190 tests across 53 packages and `go vet ./...` passed.
  - Reopened (2026-08-24): persistence and validation exist, but the flat stop rows omit the accepted reference filmstrip, selected-stop editor, narration status, character countdown, and next-stop workflow.
  - Partial verification (2026-08-24): isolated browser runs passed three consecutive additions, filmstrip selection, distinct persisted Unicode narration, narration countdown/status, picker query and selected-stop preservation, ordering, reload recovery, and overflow-free reference geometry at 390px, 834px, 1440px, dark theme, and 200% text. Final completion remains gated on a clean JavaScript-disabled mutation journey whose previous harness runs reached the admission limiter.
  - Verification (2026-08-24): one fresh JavaScript-disabled Chromium context retained a single development session through three ordinary 303 additions, filmstrip GET selection, distinct Unicode narration, reorder, removal, replacement, metadata, publication, cookie-less token opening, and ordinary slideshow navigation. Database owner/draft evidence confirmed one stable identity; earlier 429 observations were traced to multiple test contexts correctly sharing the three-new-drafts-per-hour client budget rather than an application defect. The production `__Host-` cookie, CSRF, origin, trusted-client, and admission controls were unchanged; 107 focused itinerary tests passed.
  - Reopened (2026-08-24): the final parity audit found that the picker promises artist search but queries titles only, and one shared 400-profile image is used where the reference and delivery contract require 200-profile picker rows and 500-profile editor plates.
  - Final verification (2026-08-24): independent fresh-state acceptance passed real-artist picker search, distinct additions, 200-profile picker and 500-profile editor images, filmstrip selection, ordering, bounded Unicode narration and autosave, removal, clear, reload recovery, metadata, and ordinary no-JavaScript mutations within the production admission controls.
- [x] 9.4 Implement immediate immutable token sharing, stated expiry, link-only or listed visibility, and the owned purge lifecycle job.
  - Previous verification (2026-08-23): tests proved guarded draft-to-pending publication, immutable random public tokens and published fields, approved/unexpired visibility, pending/rejected denial, expiry responses, publication budgets, transactional purge, cascaded stops, redacted run logging, and central daily job registration.
  - Reopened (2026-08-24): pending moderation contradicts the proposal, itinerary requirement, and accepted reference by issuing a public-looking URL that returns 404. Publication must make the token slideshow immediately readable while preserving validation, abuse controls, the listed/link-only choice, expiry, and lifecycle deletion.
  - Verification (2026-08-24): a bounded idempotent migration moved legacy pending records to the public state and the supplied previously failing token became cookie-less HTTP 200. Fresh isolated browser publication proved immediate immutable link-only and listed tokens, exact one-year expiry, saved narration, ordinary stop navigation, link-only index exclusion, listed index inclusion, missing-token denial, and private no-store responses; 42 focused publication checks and 138 itinerary/migration package tests passed.
  - Reopened (2026-08-24): the publication confirmation still omits the reference copy control, maker, and visibility rows, so the completed workflow does not yet meet the full reference presentation contract.
  - Final verification (2026-08-24): independent fresh-state browser acceptance passed keyboard-selected listed and touch-selected link-only visibility, immediate immutable token publication, token-scoped index inclusion/exclusion, copy/maker/expiry/visibility confirmation, exact one-year lifetime, ordinary no-JavaScript publication, owned daily purge registration, and correct fourth-draft admission rejection.
- [x] 9.5 Implement the public itinerary page and slideshow with server fallback, keyboard navigation, Escape, prefetch, and deliberate image viewer.
  - Verification (2026-08-23): server/template and eight frontend tests passed for one-stop URLs, ordinary previous/next/exit links, editable-control-safe Arrow/Escape enhancement, at most two neighbour prefetches, 1400/2000 no-upscale image profiles, and private no-store responses.
  - Reopened (2026-08-24): the current slideshow remains an ordinary page rather than the reference fixed dark two-panel viewer and omits its progress strip/direct stop links, date/school metadata, populated artist byline, narration attribution, and keyboard guidance.
  - Final verification (2026-08-24): independent acceptance passed the fixed dark two-panel viewer, progress/direct links, metadata/byline/narration/guidance, synchronous idempotent Arrow/Escape handling on direct/reload/direct-stop and HTMX lifecycles, editable-control guards, bounded prefetch, server-rendered no-JavaScript links, 1400 display and image-level 2000/original zoom, and stable full-viewport geometry at 390px, 834px, 1440px, 200% text, dark, and reduced motion.
- [x] 9.6 Add migration, workflow, abuse-control, expiry-job, and critical browser-journey tests.
  - Verification (2026-08-24): the hardened critical Playwright journey passed 13/13 with no skips on a new isolated server/data directory; 316 migration/workflow/lifecycle/handler/template tests, 19 frontend tests, vet, build, and Biome also passed. Three additional fresh isolated runs reproduced the green browser result without bypassing the three-draft/three-publication budgets.

## 10. Postcards, guestbook, feedback, and music

- [x] 10.1 Implement postcard composition, validation, honeypot/rate limiting, persisted delivery work with encrypted recoverable token storage, and hash-only recipient-page token routing.
  - Verification (2026-08-23): workflow, migration, race, security, and real-data browser/Mailpit checks proved published-artwork validation, trusted-client abuse controls, persistence before delivery, 256-bit recipient tokens, hash-only lookup, AES-256-GCM restricted envelopes, no plaintext logging/storage, ordinary no-JavaScript submission, and secure recipient routing.
- [x] 10.2 Implement idempotent postcard delivery, retry/recovery handling, versioned-key rotation and rewrapping, confirmation, and recipient reading pages.
  - Verification (2026-08-23): atomic claims, stable message IDs, retry/dead-letter classification, bounded transactional key rewrapping and purge, terminal envelope deletion, confirmation and no-store/no-referrer recipient pages passed focused/race/security suites. A fresh producer-seeded run persisted work before sending exactly one Mailpit message whose stable recipient URL rendered HTTP 200; an arbitrary token returned 404.
- [x] 10.3 Implement moderated guestbook submission, approval visibility, text search, year navigation, and historical metadata rendering.
  - Verification (2026-08-23): approved-only archive/search/year queries, bounded trusted-client submission control, archival disclosure, 90-day private retention, and atomic irreversible approved-entry withdrawal redaction passed migration, repository, handler, template, security, and browser checks. A no-JavaScript real-data submission redirected with confirmation, remained unreviewed, and did not enter the public archive.
- [x] 10.4 Replace the floating feedback dialog entry point with an ordinary link to the configured public GitHub issue list and verify JavaScript-independent navigation.
  - Verification (2026-08-21): the shared floating control now uses the exact public GitHub issue-list URL as an ordinary link and retains its responsive visual position without HTMX or dialog attributes; 7 focused layout tests, Templ/frontend/application builds, Biome, and scoped diff checks passed.
  - Verification (2026-08-21): 4 focused Chromium feedback/cookie-consent checks passed against a fresh asset-matched build, including intercepted navigation to the exact external URL with JavaScript disabled.
- [x] 10.5 Implement the named period-music player route/window, card state synchronisation, fallback link, and blocked-popup notice.
  - Verification (2026-08-23): explicit song/composer publication checks, registered player route, artist/Dual/postcard card projections, named-window reuse, origin/source validation, blocked-popup notice, ordinary-link fallback and no-autoplay behavior passed focused tests and six real-browser journeys.
- [x] 10.6 Add privacy, retention, moderation, external-delivery, keyboard, and no-JavaScript regression coverage for public participation.
  - Verification (2026-08-23): independent security review accepted token confidentiality, key lifecycle, delivery ambiguity handling, trusted request identity, moderation/redaction, bounded lifecycle jobs and music messaging. The final gate passed 1,163 Go tests across 53 packages, race suites, vet/build, Templ/frontend/Biome/lint/diff checks, Mailpit delivery, three responsive viewports and JavaScript-disabled postcard/guestbook/music paths.

## 11. Release hardening and acceptance

- [x] 11.1 Define retention outcomes, redaction rules, rate limits, moderation ownership, and lifecycle-job observability for all anonymous public data.
  - Verification (2026-08-23): itineraries, postcards and guestbook now have explicit owner, moderation/visibility, retention or archival-withdrawal outcome, irreversible redaction/purge behavior, trusted-client limits, bounded jobs and redacted count-only run logging. Independent security review and transactional/race tests confirmed active work and unresolved dead letters are retained while expired eligible data drains within per-run budgets.
- [ ] 11.2 Run migrations and seed a fresh release data directory from the approved real dataset and staged storage.
  - Partial verification (2026-08-23): the actual producer database imported into a fresh isolated WGA data directory in 21.96 seconds and a second run was a clean 0.13-second no-op. Target counts were 52,866 artworks, 957 locations, 1,307 selections, and 5,829 artists; integrity, foreign keys, duplicate IDs/source rows, selection provenance/membership, dates, and 44,590 location relations matched the source.
  - Previous blockers (2026-08-23): the then-current producer database hash `61f236…b7b106` differed from the manifest-declared `8b8d1f…d4d5e9`, staged PocketBase storage and operator provisioning evidence were unverified, and that producer revision supplied zero art periods and tone values. The isolated database is retained at `/tmp/opencode/wga-import-rerun-20260823/data.db`.
  - Partial verification (2026-08-24): the updated producer seed imported 52,866 artworks and 32 art periods into fresh isolated data, including one truthful image-less artwork and 52,865 declared image files whose staged paths all exist. Source IDs/provenance/filenames, integrity and foreign keys matched; repeat migration was a clean no-op; focused seed/migration checks, `go test ./... -cover` with 1,204 tests across 53 packages, `go vet ./...`, and a fresh build passed.
  - Partial verification (2026-08-24): a read-only release-bundle sweep matched the manifest database (`fae70c…cee35`), schema (`6d3afb…066f`), and 542,320-file storage (`383b66…7dcc`) hashes; database integrity and 35 foreign keys passed; all 52,865 declared artwork originals, 5,593 portraits, 483 music files, 687,245 source-eligible artwork renditions, portrait renditions, and 52,865 image-derived colour profiles were present with no upscales or colour-hash mismatches. One artwork is truthfully image-less and one harmless orphan colour-cache file has no database row.
  - Scope decision (2026-08-24): tone-keyword import, filtering, and record presentation are deferred to a later source-backed change and are not release blockers. Image-derived palettes and palette-similarity relationships remain required.
  - Blockers (2026-08-24): deployment operator/reviewer, installed database destination, remote S3 destination/count/bytes/content evidence, and release identity remain unrecorded and require explicit deployment authorisation. The producer working tree is not clean, so the bundle is not yet pinned to an immutable producer commit or tag.
- [ ] 11.3 Verify all release routes and assets against the complete reference inventory using real production-shaped data.
  - Partial verification (2026-08-25): a six-part static comparison of every implemented page and shared component in `internal/assets/templ` against `wga-visual-overhaul/project/templ` (STYLE-GUIDE.md/FEATURES.md as the reference contract) found the token, breakpoint, dark-mode-ordering, keyboard-layer, and dual-mode pane-query plumbing all match the reference value-for-value, but surfaced roughly 50 visual-fidelity gaps, four of them site-wide. This task remains open; the Chrome screenshot comparison (11.4) is still required to close it.
  - Site-wide: `.text-muted`, `.text-faint`, `.text-faint-2`, and `.border-control` — the reference's plain CSS classes layered over the already-correct `--wga-*` custom properties — are referenced 162 times across 36 templ files but defined nowhere in `resources/css/style.pcss` or the compiled stylesheet (confirmed by direct grep of both); every element using them silently loses its intended grey rank or control-boundary contrast. `--font-sans` also leads with the "Noto Sans" webfont where the reference specifies system fonts only (STYLE-GUIDE.md §3: "no webfont"), a site-wide typeface mismatch. Bare-px font sizes recur in place of the `--t-*` scale across several components and a few page titles that lost their responsive step entirely.
  - Component/page drift: the itinerary tray is not inverted and drops its CLEAR control and "ARRANGE & NARRATE →" copy (clearing itself still works, from the builder page, so this is visual, not functional); the dialog close control moved from the header rule to a floating corner ✕; Statistics has no no-JavaScript fallback for either chart (only its data tables); Guestbook, Glossary, and Licences hand-roll their page head instead of calling the shared `PageHead` component; Contributors renders a GitHub icon glyph, the one glyph-rule violation found ("no icon glyphs where a word fits"); Guided Tours' four-column facts block carries 5 items instead of 4, breaking its intentional visual pairing with the Itineraries facts block; Selection shows the wrong section number (03 instead of 21) and a two-column layout where the reference is single-column; Timeline implements one lane where the reference specifies six (reduced scope, not itemized further).
  - Layout defect (2026-08-25): the itinerary tray's bottom-page layout reservation only covers `<main>` (`itineraryMainClass` adds `pb-20` to `#mc-area` in `layouts/layout.templ:127-129`, per task 9.2's "layout reservation across supported surfaces"). The toast container (`#toast-container`, `layouts/layout.templ:100`) is a separate fixed-position sibling outside `<main>` with no matching offset, so a toast fired while the tray is visible can render partly behind the fixed tray bar. Needs bottom padding/offset applied to `#toast-container` conditional on tray visibility, mirroring `itineraryMainClass`.
- [ ] 11.4 Add Chrome screenshot comparison at 390px, 834px, and 1440px for each approved public reference state.
- [ ] 11.5 Run supported-browser functional checks and keyboard, NVDA/Firefox, VoiceOver/Safari macOS/iOS, and TalkBack/Chrome Android acceptance checks.
- [ ] 11.6 Run the focused Go, frontend build, browser, migration, and release-data verification suites; record results before release promotion.
