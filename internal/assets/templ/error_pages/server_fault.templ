package error_pages

import (
	"github.com/blackfyre/wga/internal/assets/templ/components"
	"github.com/blackfyre/wga/internal/assets/templ/layouts"
)

templ ServerFaultPage() {
	@layouts.LayoutMain() {
		<head><title>500 — Service unavailable</title></head>
		@ServerFaultBlock()
	}
}

templ ServerFaultBlock() {
	<section class="container mx-auto px-4 py-20 md:px-0 md:py-32">
		@components.PageHead("500 — SERVICE ERROR", "The archive could not complete that request.", "Please try again shortly. If the problem continues, send us a feedback report.")
		<div class="flex flex-wrap gap-2.5">
			<a href="/" class="wga-button wga-button-primary font-mono text-(length:--t-11) tracking-[1.5px]">RETURN TO THE GALLERY →</a>
			<a href="/artworks" class="wga-button font-mono text-(length:--t-11) tracking-[1.5px]">BROWSE ALL ARTWORKS →</a>
		</div>
	</section>
}
